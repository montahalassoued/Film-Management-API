spring.application.name=moviemanagement

# Disable H2 Console (optional)
spring.h2.console.enabled=false

# --- CONFIGURATION MYSQL (Replace H2 with MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/movie_db?useSSL=false&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=montaha
spring.datasource.password=montaha123

# --- JPA / Hibernate for MySQL ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
